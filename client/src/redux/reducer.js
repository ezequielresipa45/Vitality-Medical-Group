
const initialState = {
    testState: [],
  };
  
 
  const reducer = (state = initialState, action) => {
    switch (action.type) {

        // cases ....
  
      default:
        return {
          ...state,
        };
    }
  };
  
  export default reducer;